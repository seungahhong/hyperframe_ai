# ── 의존성 / 캐시 / OS ──────────────────────────────
node_modules/
.cache/
*.log
.DS_Store
.env
.env.local

# ── 파이프라인 산출물(프로젝트 폴더 전체 제외) ──────────
# projects/ 안은 모두 동적 생성물로 보고 통째로 제외한다.
# (script.json 등 입력을 보존하려면 별도 위치에 백업하거나 negate 규칙을 추가)
projects/

# ── 임시 학습 스캐폴드 ───────────────────────────────
_sample/
.hf-sample/
